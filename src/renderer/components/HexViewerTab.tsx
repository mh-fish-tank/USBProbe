import React from 'react'

export function HexViewerTab({ device }: { device: any }): React.ReactElement {
  return <div style={{ color: 'var(--text-muted)', padding: 20 }}>Hex Viewer placeholder</div>
}
